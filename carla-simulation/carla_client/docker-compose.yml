# Auto-generated by `../../openadstack/openadstack/utils/scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/carla-simulator/blob/v0.10.0-1.1.1-carla-client/deployment/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/carla-simulator/compose:v0.10.0-1.1.1-carla-client

services:

  carla-client:
    profiles:
      - traffic
    depends_on:
      carla-server:
        condition: service_healthy
    extends:
      file: ../../openadstack/openadstack/utils/compose/docker-compose.template.yml
      service: gpu-x11-service
    image: ghcr.io/openads-project/carla-simulator:client_v0.10.0-1.1.1
    environment:
      # CARLA_HOST: carla-server
      CARLA_HOST: ${CARLA_HOST:-carla-server}
      # CARLA_PORT: 2000
      CARLA_PORT: ${CARLA_PORT:-2000}
    # command:
    #   - sleep
    #   - infinity
    command:
      - /bin/bash
      - -ic
      - |
        exec python3 /set_environment.py
    volumes:
      - ./set_environment.py:/set_environment.py:ro
    stop_grace_period: 30s
