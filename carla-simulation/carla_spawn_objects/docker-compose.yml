# Auto-generated by `../../openadstack/openadstack/utils/scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/carla-ros-bridge/blob/main-carla_spawn_objects/deployment/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/carla-ros-bridge/compose:main-carla_spawn_objects

services:

  carla-spawn-objects:
    profiles:
      - carla
    depends_on:
      carla-server:
        condition: service_healthy
      carla-ros-bridge:
        condition: service_started
    extends:
      file: ../../openadstack/openadstack/utils/compose/docker-compose.template.yml
      service: ros2-service
    image: ghcr.io/openads-project/carla-ros-bridge:v0.10.0-1.1.0
    environment:
      # --- name ------
      NAMESPACE: /
      NAME: carla_spawn_objects
      # --- other -----
      LOG_LEVEL: ${LOG_LEVEL:-info}
      USE_SIM_TIME: ${USE_SIM_TIME:-true}
      # OBJECTS_DEFINITION_FILE: objects.json
      OBJECTS_DEFINITION_FILE: ${SENSORS:-objects.json}
      # OBJECTS_DIRECTORY: /docker-ros/ws/install/carla_spawn_objects/share/carla_spawn_objects/config
      OBJECTS_DIRECTORY: /config/objects
      # BLUEPRINTS_DIRECTORY: /docker-ros/ws/install/carla_spawn_objects/share/carla_spawn_objects/config/blueprints
      BLUEPRINTS_DIRECTORY: /config/blueprints
      ROLE_NAME: ego_vehicle
      # SPAWN_POINT_EGO_VEHICLE: None
      SPAWN_POINT_EGO_VEHICLE: ${SPAWN_POINT:-None}
      SPAWN_SENSORS_ONLY: false
      CONTROL_ID: control
    command:
      - /bin/bash
      - -ic
      - |
        ros2 launch carla_spawn_objects carla_example_ego_vehicle.launch.py \
          log_level:="$${LOG_LEVEL}" \
          use_sim_time:="$${USE_SIM_TIME}" \
          objects_definition_file:=$${OBJECTS_DEFINITION_FILE} \
          objects_directory:=$${OBJECTS_DIRECTORY} \
          blueprints_directory:=$${BLUEPRINTS_DIRECTORY} \
          role_name:=$${ROLE_NAME} \
          spawn_point_ego_vehicle:=$${SPAWN_POINT_EGO_VEHICLE} \
          spawn_sensors_only:=$${SPAWN_SENSORS_ONLY} \
          control_id:=$${CONTROL_ID}
    volumes:
      - ./objects:/config/objects:ro
      - ./blueprints:/config/blueprints:ro
