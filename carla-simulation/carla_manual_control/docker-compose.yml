# Auto-generated by `../../openadstack/openadstack/utils/scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/carla-ros-bridge/blob/v0.10.0-1.1.1-carla_manual_control/deployment/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/carla-ros-bridge/compose:v0.10.0-1.1.1-carla_manual_control

services:

  carla-manual-control:
    profiles:
      - carla
    depends_on:
      carla-server:
        condition: service_healthy
    extends:
      file: ../../openadstack/openadstack/utils/compose/docker-compose.template.yml
      service: ros2-x11-service
    image: ghcr.io/openads-project/carla-ros-bridge:v0.10.0-1.1.1
    environment:
      # --- name ------
      NAMESPACE: /
      NAME: carla_manual_control
      # --- other -----
      LOG_LEVEL: ${LOG_LEVEL:-info}
      USE_SIM_TIME: ${USE_SIM_TIME:-true}
      ROLE_NAME: ego_vehicle
      WIRELESS_CONTROLLER: false
      WINDOW_WIDTH: 800
      WINDOW_HEIGHT: 600
    command:
      - /bin/bash
      - -ic
      - |
        ros2 launch carla_manual_control carla_manual_control.launch.py \
          log_level:="$${LOG_LEVEL}" \
          use_sim_time:="$${USE_SIM_TIME}" \
          role_name:=$${ROLE_NAME} \
          wireless_controller:=$${WIRELESS_CONTROLLER} \
          window_width:=$${WINDOW_WIDTH} \
          window_height:=$${WINDOW_HEIGHT}
    privileged: true
