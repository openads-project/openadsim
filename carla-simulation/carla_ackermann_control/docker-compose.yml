# Auto-generated by `../../openadstack/openadstack/utils/scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/carla-ros-bridge/blob/v0.10.0-1.1.1-carla_ackermann_control/deployment/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/carla-ros-bridge/compose:v0.10.0-1.1.1-carla_ackermann_control

services:

  carla-ackermann-control:
    profiles:
      - carla
    depends_on:
      carla-server:
        condition: service_healthy
    extends:
      file: ../../openadstack/openadstack/utils/compose/docker-compose.template.yml
      service: ros2-service
    image: ghcr.io/openads-project/carla-ros-bridge:v0.10.0-1.1.1
    environment:
      # --- name ------
      NAMESPACE: /
      NAME: carla_ackermann_control
      # --- inputs ----
      # ACKERMANN_COMMAND_TOPIC: 
      ACKERMANN_COMMAND_TOPIC: "/control/ackermann_trajectory_control/controls"
      # --- outputs ---
      # VEHICLE_CONTROL_COMMAND_TOPIC: 
      VEHICLE_CONTROL_COMMAND_TOPIC: "/carla/ego_vehicle/vehicle_control_cmd_automated"
      # --- other -----
      LOG_LEVEL: ${LOG_LEVEL:-info}
      USE_SIM_TIME: ${USE_SIM_TIME:-true}
      # PARAMS: /docker-ros/ws/install/carla_ackermann_control/share/carla_ackermann_control/settings.yaml
      PARAMS: /docker-ros/ws/install/carla_ackermann_control/share/carla_ackermann_control/config/params.yml
      ROLE_NAME: ego_vehicle
      CONTROL_LOOP_RATE: 0.05
    command:
      - /bin/bash
      - -ic
      - |
        ros2 launch carla_ackermann_control carla_ackermann_control.launch.py \
          log_level:="$${LOG_LEVEL}" \
          use_sim_time:="$${USE_SIM_TIME}" \
          params:=$${PARAMS} \
          role_name:=$${ROLE_NAME} \
          control_loop_rate:=$${CONTROL_LOOP_RATE} \
          $${ACKERMANN_COMMAND_TOPIC:+ackermann_command_topic:=$${ACKERMANN_COMMAND_TOPIC}} \
          $${VEHICLE_CONTROL_COMMAND_TOPIC:+vehicle_control_command_topic:=$${VEHICLE_CONTROL_COMMAND_TOPIC}}
    # volumes:
    #   - ./settings.yaml:/docker-ros/ws/install/carla_ackermann_control/share/carla_ackermann_control/settings.yaml
    volumes:
      - ./params.yml:/docker-ros/ws/install/carla_ackermann_control/share/carla_ackermann_control/config/params.yml
