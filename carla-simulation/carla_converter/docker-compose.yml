# Auto-generated by `../../openadstack/openadstack/utils/scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/carla_converter/blob/v1.3.2/deployment/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/carla_converter/compose:v1.3.2

services:

  carla.carla-converter:
    profiles:
      - carla
    depends_on:
      carla-server:
        condition: service_healthy
    extends:
      file: ../../openadstack/openadstack/utils/compose/docker-compose.template.yml
      service: ros2-service
    image: ghcr.io/openads-project/carla_converter:v1.3.2
    environment:
      # --- name ------
      # NAMESPACE: /
      NAMESPACE: /carla
      NAME: carla_converter
      # --- outputs ---
      OBJECT_LIST_TOPIC: ~/object_list
      TRAFFIC_LIGHTS_TOPIC: ~/traffic_lights
      MAP_INFO_TOPIC: ~/map_info
      # --- other -----
      LOG_LEVEL: ${LOG_LEVEL:-info}
      USE_SIM_TIME: ${USE_SIM_TIME:-true}
      PARAMS: /docker-ros/ws/install/carla_converter/share/carla_converter/config/params.yml
    command:
      - /bin/bash
      - -ic
      - |
        ros2 launch carla_converter carla_converter.launch.py \
          namespace:=$${NAMESPACE} \
          name:=$${NAME} \
          log_level:=$${LOG_LEVEL} \
          use_sim_time:=$${USE_SIM_TIME} \
          params:=$${PARAMS} \
          object_list_topic:=$${OBJECT_LIST_TOPIC} \
          traffic_lights_topic:=$${TRAFFIC_LIGHTS_TOPIC} \
          map_info_topic:=$${MAP_INFO_TOPIC}
    # volumes:
    #   - ./params.yml:/docker-ros/ws/install/carla_converter/share/carla_converter/config/params.yml
    volumes:
      - ./params.yml:/docker-ros/ws/install/carla_converter/share/carla_converter/config/params.yml
