services:

  planning.lanelet2-route-planning:
    profiles:
      - planning
      - ml-planning
    extends:
      file: ../../openadstack/planning/lanelet2_route_planning/docker-compose.yml
      service: planning.lanelet2-route-planning
    environment:
      PARAMS: /params.yml
    volumes:
      - ./params.yml:/params.yml
