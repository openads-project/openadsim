# Auto-generated by `../../openadstack/openadstack/utils/scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/monitoring/blob/v26.9.4/deployment/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/monitoring:compose-v26.9.4

services:

  sumo-control-active-bridge:
    profiles:
      - sumo
    depends_on:
      - sumo-interface
    extends:
      file: ../../openadstack/openadstack/utils/compose/docker-compose.template.yml
      service: ros2-service
    restart: unless-stopped # requires incoming topic
    image: ghcr.io/openads-project/monitoring:v26.9.4
    environment:
      # --- name ------
      NAMESPACE: /
      # NAME: monitoring
      NAME: sumo_control_active_bridge
      # --- other -----
      LOG_LEVEL: ${LOG_LEVEL:-info}
      USE_SIM_TIME: ${USE_SIM_TIME:-false}
      CONFIG: ${CONFIG:-/docker-ros/ws/install/monitoring/share/monitoring/config/conf.rviz}
    # command:
    #   - /bin/bash
    #   - -ic
    #   - |
    #     ros2 launch monitoring monitoring.launch.py \
    #       namespace:=$${NAMESPACE} \
    #       name:=$${NAME} \
    #       log_level:=$${LOG_LEVEL} \
    #       use_sim_time:=$${USE_SIM_TIME} \
    #       config:=$${CONFIG}
    command: >
      ros2 run topic_tools transform
        /sumo/manual_control_override
        /control/active
        std_msgs/Bool
        "std_msgs.msg.Bool(data=not m.data)"
        --import std_msgs
